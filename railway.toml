# Default Railway config. For persistent volumes + template settings, prefer:
#   deploy/railway/railway.json
[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/remote-agent/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
requiredMountPath = "/home/workspace"
